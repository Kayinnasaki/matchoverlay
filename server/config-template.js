const config = {
    // Path to Cert for SSL. Leave blank to disable SSL ie: privateKey: "",
    privateKey: "${PRIVATE_KEY_PATH}",
    certificate: "${CERTIFICATE_PATH}",
    // Challonge API Key
    apikey: '${CERTIFICATE_PATH}'
};

module.exports = config;

