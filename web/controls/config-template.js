const config = {
    // Address of Websocket Server
    ServerUrl: "${WEBSOCKET_HOST}",
    
    // wss for SSL ws for no SSL
    ServerSSL: "${SERVER_PROTOCOL}",

    ControlPort: "${CONTROL_PORT}",
    ScoreboardPort: "${SCOREBOARD_PORT}",
    
    //Url for Scoreboard link
    ScoreboardUrl: "${SCOREBOARD_URL}",
};